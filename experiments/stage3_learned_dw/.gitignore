# Phase 1 artefacts that are regenerated by every run; only the JSON verdict
# is worth tracking. dw_hat.pt is small but rebuilds in <40s; PPO zips are
# bigger and only useful for reloading -- the eval table in JSON is the
# real deliverable.
phase1_results/dw_hat.pt
phase1_results/ppo_*.zip
__pycache__/
